---
name: findings-ledger
description: Rule 279 v2 schema — quality findings ledger for ai-real-estate-assistant
metadata:
  type: project
---

# Quality Findings Ledger — ai-real-estate-assistant

Rule 279 v2 schema. Rows are written automatically by `/finish`, `/finito`, `/end`,
`/burst`, and `/prerelease`; the agent and operator triage them via `/findings-review`,
`/findings-triage`, and `/findings-promote`. Status ∈ `open` · `doing` · `postponed —
<reason>` · `blocked — <reason>` · `promoted` · `closed`. `Verified` is the date the
claim was last re-checked (`—` if never). Escape a literal `|` inside a cell as `\|`.

| ID | Date | Sev | Status | Location | Claim | Next step | Source | Verified |

## Open

| ID | Date | Sev | Status | Location | Claim | Next step | Source | Verified |
| F-20260905-1 | 2026-09-05 | med | closed — resolved by PRs #290–#294 on `dev@82632e8` | apps/api/tests/unit/test_port_config.py + others | Original claim referenced `main@50e98ad` anchor (41 pre-existing unit test failures). Anchor stale by 5 commits since main is now at `52fea56`. Cherry-picked `3ebcda5` (visibility work — added apps/web/src/lib/structured-data.ts, llms.txt, sitemap cleanup, etc.), `fda9063` (xfail PR #291 with F-20260905-4..12 references), `3784b5f` (dev-sync PR #292 — deterministic async-mocks + Linux CI timeout fixes for test_health), and `52fea56` (PR #294 middleware/proxy migration) onto `dev@82632e8` on 2026-09-07. Verification: `cd apps/api && pytest tests/unit -p no:cacheprovider -q --tb=no` returns **6272 passed, 0 failed, 17 xfailed (documented)**, 31 skipped, 745 warnings** — zero unaddressed failures. The 41 baseline failures collapsed to 0 via the cherry-picks; remaining 17 xfails are pre-existing documented tests (test_user_activity_search_event PostgreSQL, mock provider factory, lead response model, route shadowing, etc.). | n/a | this session `/goal` cleanup | 2026-09-07 |
| F-20260905-6 | 2026-09-07 | low | closed — TaskMaster #14/#15/#16 unblocked by user authorization | `.taskmaster/tasks/tasks.json` lines (see IDs) | TaskMaster #14/#15/#16 visibility-growth chain (`14: Launch AI-developer content after technical PR` → `15: Run community and directory distribution sprint` → `16: Optimize channels toward 50 Stars/day`) was previously `blocked` per F-20260905 blocker notes ("Hard Rule 1 — needs explicit operator push/PR approval before agent can dispatch outward-facing work"). User authorized in 2026-09-07 session: "Authorize (mark in-progress)". All 3 transitioned `blocked → in-progress` via `mcp__mgmt-taskmaster__set_task_status`. The visibility-growth sprint now proceeds per Hard Rule 1 (operator push OK). | n/a (operator authorized in-session) | this session `/goal` cleanup | 2026-09-07 |
| F-20260905-2 | 2026-09-06 | med | closed — filter pruned by commit 0465d0c on dev | .github/workflows/ci.yml | Original `if: github.repository_owner == 'NestLab-Tech'` filter was added by commit `b6664a6 ci: adapt workflows for NestLab-Tech private repo (Task #120)` (pre-2026-09-05). Subsequently pruned by commit `0465d0c chore(repo): prune references from tracked config + CI` (post-2026-09-05, on dev branch). Current ci.yml uses branch-based gate (`github.ref in ['dev','main']`) instead of owner filter — 15 jobs run on both repos. Verify with `git show dev:.github/workflows/ci.yml | grep -E 'NestLab|repository_owner'` returns no NestLab-Tech filter. Tier 1 fix from the plan was no-op since filter already pruned; ledger closure is the actual completion. | n/a | this session followup (Tier 1, dev branch only) | 2026-09-06 |
| F-20260905-4 | 2026-09-06 | low | closed — fixed via PR #294 | apps/web/src/middleware.ts | PR #292 task #12 (93c164f feat(ci): Next.js 16 themeColor + middleware→proxy migration) created `apps/web/src/proxy.ts` but forgot to delete the old `apps/web/src/middleware.ts`. Next.js 16 detects BOTH files and fails the build: `Error: Both middleware file './src/src/middleware.ts' and proxy file './src/src/proxy.ts' are detected.` Blocked publish-ghcr.yml #34036204387 (v5.1.5 first attempt). Fix: PR #294 `fix(web): remove stale middleware.ts`. After merge, deleted + re-pushed tag as v5.1.5.1 (tag increment avoids the cached deadlocked run state from v5.1.5). publish-ghcr.yml #34046005946 succeeded for v5.1.5.1 (~18 min total). | n/a | this session `/finito` follow-up | 2026-09-07 |
| F-20260905-5 | 2026-09-06 | low | closed — v5.1.5.1 GHCR images built + pushed | ghcr.io/AleksNeStu/ai-real-estate-assistant/{frontend,backend}:v5.1.5.1 | After PR #294 merge (commit 52fea56ef16f6bcdb407b5913d034e2c2b45c1ca), tagged v5.1.5.1 (avoiding v5.1.5 cached run state) and pushed to all 3 mirrors. publish-ghcr.yml #34046005946 ran for ~18 min: publish-backend ✅ success, publish-frontend ✅ success. Local docker buildx verification: `time docker buildx build --tag ai-real-estate-assistant-frontend:test --file deploy/docker/Dockerfile.frontend --load .` completed in 5m22s. | n/a | this session `/finito` | 2026-09-07 |
| F-20260912-9 | 2026-09-12 | low | closed — tag-only release path accepted; merge-commit path blocked by repo rule 'no merge commits on main'; PR #312 closed | branches `dev` + `main` | `dev` (d21eefee) and `main` (52fea56e) have unrelated git histories (`git merge-base` returns empty). v5.1.5.2 tag at `ed987541` on all 3 mirrors + GHCR images + GitHub Release page published. PR #312 (dev→main, `mergeable=CONFLICTING`) closed in 2026-09-13 session. Plan A (merge commit via API) attempted: merge commit `c5d3481cf961a23879add1268bc80430bac59d37` created via `POST /repos/.../git/commits` with parents `[52fea56e, d21eefee]` and tree `a3b1c803` (== dev tree); `PATCH /repos/.../git/refs/heads/main` rejected with HTTP 422 — repository rule "This branch must not contain merge commits." (the rule pre-dated this session; PR #292's merge commit pre-dates the rule). Plan C fallback applied: tag-only release remains canonical; v5.1.5.2 reachable via `refs/heads/dev` (= `d21eefee`); merge-commit object `c5d3481d` orphaned (will be GC'd by GitHub). | none — release artifacts intact (tag, Release page, GHCR images), history divergence accepted as known limitation | 2026-09-12 close-out, 2026-09-13 resolution session | 2026-09-13 |
| F-20260912-10 | 2026-09-12 | info | closed — work done in commit 4f0b6e4b before ledger entry was filed | docs/operations/security/accepted-risks.md | `accepted-risks.md` re-reviewed (file header: `**Last reviewed:** 2026-09-12`, previous review `2026-08-26`); CVE-2026-45829 / GHSA-f4j7-r4q5-qw2c added as explicit "NOT APPLICABLE" prose block at lines 65-69 (chromadb vulnerable range 1.0.0-1.5.9 per OSV; project pins `chromadb>=0.5.0,<0.6.0` in `apps/api/pyproject.toml`, locked to 0.5.23 in `apps/api/uv.lock` — pin is below vulnerable range, so Dependabot correctly does not surface CVE-2026-45829 as an alert). 0 open Dependab alerts on the repo at review time. Commit message of 4f0b6e4b: `docs(security): accepted-risks re-review — 0 open alerts, add CVE-2026-45829 not-applicable note`. Row not filed during the previous session's close-out; retroactively added here. | none — work complete in commit 4f0b6e4b | 2026-09-12 close-out, 2026-09-13 retroactive add | 2026-09-13 |
| F-20260912-11 | 2026-09-12 | med | blocked — out of scope (pre-existing backlog, not introduced this session) | `.taskmaster/tasks/tasks.json` | 7 TaskMaster pending tasks: #11 review (audit frontend skipLibCheck suppression), #12 (Next.js 16 metadata cleanup re-verify post-F-20260905-4), #13 (open/merge technical visibility PR — operator gated), #14-#16 (visibility chain 10/20/50 Stars/day — operator gated), #17 (next-intl SSG missing translations), #18 (untrack .mcp.json from git), #20 (refactor 2 xfail health-check tests — 2 attempts reverted per 906d1ae). Operator-gated tasks (#13-#16) blocked by Hard Rule 1 (no outward-facing work without explicit operator PR/push authorization); engineering items (#12, #17, #18, #20) pre-existing, not touched this session. | pick up in next session per priority (engineering items first; visibility items wait for operator OK) | this session pre-existing backlog | 2026-09-12 |
| F-20260913-1 | 2026-09-13 | info | closed — TaskMaster #18 / #20 individually reviewed and dispositioned in this session | `.taskmaster/tasks/tasks.json` (#18, #20) | TaskMaster #18 ("Untrack and protect .mcp.json configuration") — verified DONE in this session: `.mcp.json` is untracked (`git ls-files .mcp.json` returns empty), gitignored (`.gitignore` line 7: `.mcp.json` with comment "AI tooling local config (never commit)"), and protected by CI guard `scripts/rule5_guard/gitignored_tracked_check.py` + `apps/api/tests/unit/test_rule5_guard.py` + `rule5-protected-paths` CI job (commit `b96b4fdf`, 2026-08-27). Task description is stale (says `.mcp.json` is "tracked despite being in .gitignore" — not true since at least `af3ce64` 2026-06-22 + `b96b4fdf` 2026-08-27). Recommendation: mark `done` in TaskMaster (mgmt-taskmaster MCP not loaded in this session; operator to apply via `set_task_status` on next MCP-enabled session). TaskMaster #20 ("Refactor 2 xfail health-check tests to deterministic async-mocks") — verified DEFERRED BY DESIGN: description field is empty (no acceptance criteria), 2 prior attempts reverted per `906d1ae` ("test(health): revert xfail refactor — CI flake not solvable in this session"). No concrete path forward without re-defining acceptance criteria; not appropriate to attempt in this or any future session without explicit operator specification. Recommendation: leave `pending` in TaskMaster (with this F-20260913-1 row as the explanation); only attempt if/when acceptance criteria are filled in. | none — review complete; #18 work confirmed (mark `done` via TaskMaster MCP at next opportunity); #20 explicitly deferred (no acceptance criteria, 2 prior reverts) | this session TaskMaster review | 2026-09-13 |
| F-20260913-2 | 2026-09-13 | low | closed — push+deploy+verify session complete; staging/prod healthy on v5.1.5.3 | 3 GitHub remotes + GHCR + Render | Local `dev` was 2 commits ahead of all 3 GitHub mirrors (`52ab4841`, `70638da7` — both docs/quality/security follow-ups). User-requested `/goal push+deploy+verify` flow executed: (1) version bump commit `b6e18ef5` (`5.1.5.2` → `5.1.5.3` in `apps/api/pyproject.toml`, root `package.json`, `apps/web/package.json`); (2) `git push main dev` cascaded to AleksNeStu/dev-scaler/nest-ai-dev via pre-push hook (all 3 confirmed at `b6e18ef5`); (3) CI run `34746235001` green on `b6e18ef5` (16 jobs: gitleaks, secret-validation, rule5-protected-paths, backend-lint, backend-tests matrix unit/integration, frontend-build, security-sast, frontend-tests, security-deps, codeql-python, codeql-javascript, container-scan, lighthouse, e2e, compose_smoke, pipeline-health — e2e took 16m27s slowest job, all green); (4) annotated tag `v5.1.5.3` created on `b6e18ef5` and pushed to all 3 mirrors; (5) publish-ghcr run `34747008501` green (publish-backend 5m40s + publish-frontend 16m31s, multi-arch amd64+arm64, tags `5.1.5.3`/`<sha>`/`latest`); (6) deploy.yml auto-fired staging via `workflow_run` from CI completion — run `34746975768` green at 08:09:15Z; (7) deploy.yml prod dispatched manually — run `34747750463` green in 67s (ci-check, validate, deploy-backend, deploy-frontend, smoke-test all ✅); (8) verification: `realestate-api-wkm7.onrender.com/health` → `HTTP 200` body `{"status":"healthy","timestamp":"2026-09-13T08:27:54.201908Z",...}`, `/api/v1/search` → `HTTP 200` `{"results":[],"count":0}`, `/api/v1/chat` → `HTTP 200` with full GLM-4.7-flash mortgage-calc response confirming end-to-end backend + agent + LLM provider chain. **Note:** Render account has only 2 services (`realestate-api`, `realestate-web`), both pinned to `branch: dev` — there is no separate production environment in this Render account. `RENDER_SERVICE_ID_API` prod-secret aliases the same services as staging. Phase 8 image-digest parity check is therefore trivially satisfied (single env). | none — session objective complete; staging/prod both running v5.1.5.3 / commit `b6e18ef5`; user to review + close | this session `/goal push+deploy+verify` | 2026-09-13 |

## Doing

## Closed

## Promoted